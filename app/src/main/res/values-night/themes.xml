<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ContactsBuddy" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/dim_red</item>
        <item name="colorPrimaryVariant">@color/dim_red</item>
        <item name="colorOnPrimary">@color/dim_red</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>

        <!-- App Font -->
        <item name="android:fontFamily">@font/sf_pro_display_regular</item>
    </style>
</resources>